<%= form_for(@news_byte) do |f| %>
  <% if @news_byte.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@news_byte.errors.count, "error") %> prohibited this news_byte from being saved:</h2>

      <ul>
      <% @news_byte.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body, :cols => 100, :rows => 20 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
